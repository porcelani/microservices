spring:
  application:
    name: zuul
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 8080

---

spring:
    profiles: docker
    boot:
        admin:
           url: http://admin:8080
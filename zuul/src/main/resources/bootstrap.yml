spring:
  application:
    name: zuul
#  cloud:
#    config:
#      uri: http://localhost:8888
server:
  port: 8080

---

spring:
    profiles: docker
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
    register-with-eureka: false
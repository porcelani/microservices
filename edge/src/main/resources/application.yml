spring:
  application:
    name: edge
server:
  port: 8070

---

spring:
  profiles: docker
  cloud:
    config:
      uri: http://config:8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
